# Frontend API URL — used by Vite at build time
# For local development, the Vite proxy handles /api → localhost:3001 automatically
# For production, set this to your server's public URL
VITE_API_URL=http://localhost:3001
